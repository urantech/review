## Общее
- Высокое качество кода
- Чистая расширяемая архитектура
- Отличное покрытие тестами
- Вижу осмысленный подход к разработке

## package config
- Конфигурации явно структурированы, это отлично!
- Вижу осознанное управления инфраструктурой
- Конфигурации атомарные и логически изолированные

## AuthController
### 👍 Хорошо:
- Явные HTTP-маппинги
- Разделение ответственности

### ⚠️ Проблемы и улучшения:
- model.addAttribute(USER, signUpUserDTO.getCopy(signUpUserDTO)): избыточно и нарушает принцип single responsibility (SRP)
- Лучше поручить сервису маппинг и валидацию, т.к. контроллер не должен модифицировать входную dto
- Вкусовщина, но PasswordEncoder.encode() как статический вызов затрудняет тестирование и подмену реализации, я бы внедрял PasswordEncoder как бин через DI
- Обработка ошибок не унифицирована. Лучше в ExceptionHandlingController централизовать все исключения
- Было бы хорошо добавить логирование для авторизации и регистрации

## ExceptionHandlingController
### ⚠️ Проблемы и улучшения:
- я бы собрал все ошибки в 1 строку, т.к. в exception.getAllErrors() может быть больше 1 ошибки
- сюда можно добавить обработку бизнес исключений (писал об этом выше)
- Сейчас лог содержит только сообщение исключения. Лучше добавить HTTP-путь

## HomeController
### 👍 Хорошо:
- Читаемый, лаконичный и не содержит логики, не свойственной контроллеру

### ⚠️ Проблемы и улучшения:
- var pageableResult = (PageableResult<List<WeatherResponseDTO>>) null. Лучше так: PageableResult<List<WeatherResponseDTO>> pageableResult (меньше кода и нет лишнего каста)

## LocationController
### 👍 Хорошо:
- Явные HTTP-маппинги
- Разделение ответственности
- Имена переменных и методов выразительны, читаемость отличная

### ⚠️ Проблемы и улучшения:
- ExceptionHandler.handle(). Хорошо, что ошибки не проглатываются, но логичнее продолжать делать это в @ControllerAdvice
- var weatherResponseDto = (WeatherResponseDTO) null - лишнее, достаточно WeatherResponseDTO weatherResponseDto;
- Потенциальный NPE в checkIfLocationIsAdded. Если weatherResponseDto.getLocationDto() вернёт null, будет NullPointerException
- Отсутствие логирования

## UserController
- Явные HTTP-маппинги
- Разделение ответственности
- Контроллер написан аккуратно и логично

### ⚠️ Проблемы и улучшения:
- Дублирование логики между addLocation() и removeLocation(). Оба метода практически идентичны. Это увеличивает риск ошибок при изменениях. Можно обобщить через приватный метод
- Отсутствует логирование
- locationNameValidator.validate() - поведение неочевидно. Кажется, что лучше определить единый стиль валидации, к примеру - валидаторы кидают ошибки, а @ControllerAdvice их ловит

## UserSessionController
### ⚠️ Проблемы и улучшения:
- Проверка model.containsAttribute(USER_SESSION). Она у тебя встречается по всему коду, в других местах она избыточна. Проверь вот что: будет ли работать, если удалить такие проверки из других контроллеров. Т.к в этом контроллере есть гарантия, что USER_SESSION будет добавлен во все модели, если cookie валиден
- Отсутствие логирования
- userSessionService.findNotExpiredById. На будущее: такой вызов будет выполняться при каждом запросе - напрашивается кэширование, чтобы снизить нагрузку на БД
- Кэшируй UUID pattern как static final, чтобы не пересоздавался при каждом запросе
- Может быть добавить логику "протухания" cookie? Если userSessionService.findNotExpiredById возвращает Optional.empty(), ты ничего не делаешь с cookie — оно остаётся у клиента

## Итог по контроллерам:
- Контроллеры написаны на зрелом уровне
- Отлично выдержан паттерн MVC без Spring Boot
- Код читаемый, последовательный, с аккуратной обработкой ошибок и централизованным подходом к сессии

## SignUpUserDTO
### ⚠️ Проблемы и улучшения:
- метод getCopy не использует переданный аргумент, это вводит в заблуждение

## WeatherResponseDtoDeserializer
### 👍 Хорошо:
- Код высокого качества, чётко структурирован

### ⚠️ Проблемы и улучшения:
- если openWeather будет возвращать неожиданные значения, то могут появиться непредвиденные исключения. В целом для пет-проект это ок, но держим в голове, что в прод варианте нужно добавить больше обработок ошибок
- JacksonException не будет выброшено

## CoordinatesDTO
### ⚠️ Проблемы и улучшения:
- Неиспользуемый внутренний класс

## WeatherDTO
### ⚠️ Проблемы и улучшения:
- Неиспользуемые методы

## Итог по дто:
- Всё под контролем: структура, типы, безопасность и устойчивость
- Вижу продуманную работу

## package utils
### 👍 Хорошо:
- код чистый, декларативный, с чётким разделением ответственности
- Пагинатор реализован чисто - молодец

### ⚠️ Проблемы и улучшения:
- наименования пакетов не принято писать в snake_case
- есть неиспользуемые константы
- При ошибке загрузки properties используется @SneakyThrows. Это удобно, но скрывает checked-ошибки при старте приложения. Я бы заменил на явную обработку
- Отсутствие логирования для критичных сценариев
- Pattern.compile можно кэшировать, чтобы не создавать при каждом вызове

## Итог по utils:
- Хороший код, достойный prod уровня
- Легко читать, сразу видно, что разработчик хочет сказать этим кодом

## package validators
### 👍 Хорошо:
- Используются кастомные аннотации и ConstraintValidator. Отличный подход

### ⚠️ Проблемы и улучшения:
- Проверка isBlank() без null-safe. Все валидаторы предполагают, что входное значение не null, возможно стоит добавить эту проверку, чтобы не получить NPE

## Итог по validators:
- Отличный, зрелый код
- После небольшого рефакторинга можно считать его prod вариантом

## package entities
### ⚠️ Проблемы и улучшения:
- В User — List<Location>, а в Location — Set<User>. Выбирай что-то одно, чтобы была симметрия
- В сущностях критично иметь переопределенные equals() и hashCode() при работе с Set, например по id, иначе можно получить неожиданный результат при кэшировании и merge

## package repositories
### 👍 Хорошо:
- В духе чистого Hibernate
- Явное управление данными
- Чистый код

## package services
### 👍 Хорошо:
- Все сервисы лаконичны, каждый отвечает за свою бизнес-роль
- Хорошая транзакционная политика
- Качественная работа с исключениями

### ⚠️ Проблемы и улучшения:
- OpenWeatherService.getWeatherInfo(List<Location>) - последовательные rest запросы в цикле. Может стать проблемой, есть смысл рассмотреть CompletableFuture.supplyAsync()
- var locationId = (Long) null можно заменить на var locationId = locationIdOptional
  .orElseGet(() -> locationService.addLocation(locationName).getId());

## Миграции
### 👍 Хорошо:
- Целостность данных соблюдена
- Правильная структура отношений

### ⚠️ Проблемы и улучшения:
- Стоит рассмотреть добавление индексов на поля, по которым осуществляется поиск

## AuthControllerIntegrationTest
### 👍 Хорошо:
- Чёткая архитектура тестового слоя
- Грамотное разделение сценариев по вложенным классам
- Использование SQL-скриптов
- Отличная полнота покрытия
- Хорошие и понятные именования методов

### ⚠️ Проблемы и улучшения:
- model().attribute(USER, new IsEqual<>(expectedSignInUserDto)). При не совпадении equals() в DTO этот тест упадёт, даже если визуально данные идентичны. Лучше использовать Matchers.samePropertyValuesAs() или usingRecursiveComparison()
- Тест объявлен как интеграционный, но AuthService замокан, то есть фактически это тест веб слоя. Можно переименовать в AuthControllerTest или убрать мок и поднимать весь контекст

## HomeControllerIntegrationTest
### 👍 Хорошо:
- плюсы, аналогичные плюсам выше

### ⚠️ Проблемы и улучшения:
- var cookie = new Cookie(USER_SESSION_COOKIE, USER_SESSION_ID); можно вынести в @BeforeEach для DRY
- я бы использовал doAnswer вместо doReturn в мокировании для RestTemplate, чтобы не использовать цикл
- Тесты могут быть нестабильными из-за того, что не фиксируется время. Рекомендую внедрять Clock для фиксации

## LocationControllerIntegrationTest
### 👍 Хорошо:
- плюсы, аналогичные плюсам выше

### ⚠️ Проблемы и улучшения:
- Дублирование моков для RestTemplate можно вынести в приватный метод
- Отсутствие проверки HTTP-кода в некоторых тестах (опционально, но желательно)

## UserControllerIntegrationTest
### 👍 Хорошо:
- плюсы, аналогичные плюсам выше

### ⚠️ Проблемы и улучшения:
- Мокирование openWeatherService.getWeatherInfo(locationName) повторяется в нескольких тестах

## AuthServiceIntegrationTest
### 👍 Хорошо:
- плюсы, аналогичные плюсам выше

### ⚠️ Проблемы и улучшения:
- В assertThrows полезно проверять не только тип исключения, но и сообщение

## Итог по тестам:
- Очень мощные и зрелые тесты. Такие увидишь не на каждом реальном проекте. Этим можно гордиться

# Анализ кода проекта

## Общие замечания
Код по всему проекту не отформатирован, что говорит о несоблюдении стандартов code style.

## По файлам

### pom.xml
Не стоит оставлять закомментированный код. Если зависимости не используются, то лучше убрать их вовсе.

### README.md
Возможно душно, но на будущее пригодится. Рано или поздно ты будешь делать документацию к проекту и стоит делать файл более понятным и емким. Можно добавить сюда описание проекта, стэк технологий и тд. Нейронки в помощь, это делается просто и быстро, но улучшает качество документации.

### postman_collection.json
Наименования файлов стоит делать без лишних символов в единообразии с проектом. Я бы назвал так: `postman_collection.json` (без лишних слов и пробелов).

### Пакет jsonUtils
Как правило в проектах не используют camelCase в именовании пакетов. В этом случае я бы создал родительский пакет `utils` и добавил бы туда 2 папки: `json` и `connection`.

## Валидаторы (validators)

### 👍 Хорошо:
- Хорошие сообщения об ошибках в ErrorInfo
- Единый подход к валидации

### ⚠️ Проблемы и улучшения:
- Наследование валидаторов — не лучшая идея. Лучше композиция. Композиция всегда лучше наследования.
- Регулярные выражения — вынести в константы с понятными названиями
- Метод `extractAndValidateRate` в `RequestExchangeRateValidator`:
    - Хрупкий парсинг тела запроса (`readLine()`)
    - Лучше использовать `req.getParameter("rate")`
- Метод `validateDecimal` — сложная логика, лучше разбить на отдельные проверки
- Пустой override `extractAndValidatePath` — избыточность. Можно не переопределять и поведение не изменится.

## CurrenciesServlet

### 👍 Хорошо:
- Четкое разделение ответственности (сервис, валидатор, писатель)
- Статус код 201 для успешного создания

### ⚠️ Проблемы и улучшения:
- Синглтон (`getInstance()`) — антипаттерн, усложняет тестирование. Лучше использовать dependency injection (в будущем Spring из коробки даст такую возможность). Как вариант без фреймворка - внедрить через конструктор.
- Валидация параметров — метод `validateParam` бросает исключение? Нужно обработать ошибку и вернуть 400 с сообщением, иначе пользователь получит 500.
- Отсутствует проверка на дубликат валюты (код должен быть уникальным). Сервис может бросить исключение — его нужно перехватить и вернуть 409 Conflict.
- Потокобезопасность — синглтон-сервис должен быть thread-safe, если используется в многопоточной среде Servlet.

**Основная проблема:** отсутствует обработка исключений.

## CurrencyServlet

### 👍 Хорошо:
- Четкое разделение ответственности
- Правильная валидация пути

### ⚠️ Проблемы и улучшения:
- Неправильный путь по REST `/currency/*`. Логичнее:
    - GET `/currencies` — все валюты
    - GET `/currencies/USD` — конкретная валюта
- Синглтон и статик — те же проблемы, что и в предыдущем классе
- Избыточный DTO — `CurrencyCodeDto` выглядит как over-engineering. Можно передавать просто String code

## ExchangeRateServlet

### 👍 Хорошо:
- Использование PATCH для частичного обновления
- Единая точка входа через `service()` для нестандартных методов

### ⚠️ Проблемы и улучшения:
- Неправильный REST-путь — `/exchangeRate/USDRUB` нестандартно. Лучше:
    - GET `/exchange-rates/USDRUB` или `/exchangeRates/USDRUB`
    - PATCH `/exchange-rates/USDRUB` или PATCH `/exchangeRates/USDRUB`
- Тот же синглтон — проблема зависимостей
- `ServletException` не будет выброшено. Его можно убрать из сигнатуры метода

## ExchangeRatesServlet

### 👍 Хорошо:
- Правильный REST-путь `/exchangeRates`
- Статус 201 для успешного создания
- Четкое разделение DTO для создания и обновления

### ⚠️ Проблемы и улучшения:
- Синглтон — та же проблема с тестированием
- Неправильное исключение в doGet: `throws ValidationException` — исключения валидации не должны выбрасываться в GET без параметров

## ExchangeServlet

`exchangeService` как зависимость точно должна быть приватной: `private ExchangeService exchangeService = ExchangeService.getInstance();`

## ServletFilter

### 👍 Хорошо:
- Централизованная обработка исключений
- Логирование ошибок
- Преобразование исключений в JSON-ответы
- Правильное использование FilterChain

### ⚠️ Проблемы и улучшения:
- Я бы установил Content-Type для JSON

## ConnectionManager

### 👍 Хорошо:
- Использование final класса для утилитного класса
- Вынос строковых констант в поля класса
- Приватный конструктор для предотвращения создания экземпляров
- Собственное исключение ConnectionException с передачей исходного исключения

### ⚠️ Проблемы и улучшения:
- Отсутствие пула соединений:
- Каждый вызов get() создает новое соединение
- Нет управления соединениями (закрытие, таймауты)
- Можно написать самому, а можно использовать библиотеку connection pool, например (HikariCP, Apache DBCP)

## PropertiesUtil

### 👍 Хорошо:
- Использование final класса для утилитного класса
- Вынос строковых констант в поля класса
- Приватный конструктор для предотвращения создания экземпляров

### ⚠️ Проблемы и улучшения:
- RuntimeException скрывает реальную проблему
- Лучше использовать кастомное исключение или более информативное RuntimeException

## Currency

### 👍 Хорошо:
- Стандартная структура POJO класса
- Присутствуют equals() и hashCode() методы

### ⚠️ Проблемы и улучшения:
- Я бы использовал lombok. Это поможет существенно снизить количество лишнего boilerplate кода

## ExchangeRate

### 👍 Хорошо:
- Стандартная структура POJO класса
- Присутствуют equals() и hashCode() методы

### ⚠️ Проблемы и улучшения:
- Я бы использовал lombok. Это поможет существенно снизить количество лишнего boilerplate кода
- в equals есть шанс нарваться на NPE, если id будет null. Лучше использовать null-safe Objects.equals

## AppException

### 👍 Хорошо:
- Абстрактный класс для иерархии исключений
- Использование ErrorInfo для стандартизации сообщений об ошибках
- Поддержка цепочки исключений
- Сериализуемость

### ⚠️ Проблемы и улучшения:
- Хорошо, что добавлен serialVersionUID, но лучше генерировать уникальный

## JsonResponseWriter

### 👍 Хорошо:
- Final класс для утилитного класса
- Приватный конструктор предотвращает создание экземпляров
- Использование Gson с pretty printing для читаемости
- Правильная установка charset (UTF-8)
- Логирование ошибок
- Использование try-with-resources

### ⚠️ Проблемы и улучшения:
- Избыточность в обработке исключений. Я бы вместо throw ex написал так: throw new IOException("Failed to write JSON response", ex);

## package amountConverter

### 👍 Хорошо:
- Интересный подход к конвертации

### ⚠️ Проблемы и улучшения:
- в названии пакета не пишем в camelCase
- предпочитаем композицию наследованию. Код тяжело читается и тяжело расширяется из-за множества child классов

## CurrencyService

### ⚠️ Проблемы и улучшения:
- buildCurrencyDto кажется лишней прослойкой. Я бы сразу вызвал DtoBuilder.buildCurrencyDto(currency);
- findByCode я бы написал более идиоматично к stream api. Все остальное лишнее. Пример:
`public CurrencyDto findByCode(CurrencyCodeDto dto) {
  return currencyDao.findByCode(dto.getCode())
            .map(this::buildCurrencyDto)
            .orElseThrow(() -> new ServiceException(ErrorInfo.CURRENCY_CODE_NOT_FOUND));
  }
`

## CurrencyDao

### 👍 Хорошо:
- Использование text blocks для SQL запросов
- Правильное использование try-with-resources
- Обработка специфичных SQLite ошибок
- Использование Optional для findByCode
- Вынесение построения объекта в отдельный метод

### ⚠️ Проблемы и улучшения:
- final static нужно поменять местами на static final
- В isConstraintUniqueError ты используешь побитовый оператор вместо булева
- Можно добавить метод isSQLite и определить из connection.getMetaData().getDriverName().toLowerCase().contains("sqlite") является ли БД SQLite

## ExchangeRateDao

### 👍 Хорошо:
- Хорошие JOIN запросы для получения связанных данных

### ⚠️ Проблемы и улучшения:
- Пусть константы будут static
- Обязательно проверь наличие уникального индекса на пару валют
